services:
  app:
    image: python:3.14-slim
    working_dir: /workspace/backend
    volumes:
      - ./:/workspace
    command: sh -c "pip install -r requirements.txt && PYTHONPATH=. uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
